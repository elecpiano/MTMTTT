<Page 
    x:Class="MeiTuTieTie.Pages.OperationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:MeiTuTieTie.Controls" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    >

    <Grid>
        <Grid x:Name="stagePanel">
            <Grid x:Name="stageBackground" Background="Wheat" PointerPressed="stageBackground_PointerPressed"/>
            <Grid x:Name="stage" />
        </Grid>
        <ListBox x:Name="picListBox" VerticalAlignment="Top" Visibility="Collapsed">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Image Width="90" Height="90" Margin="4" Source="{Binding}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ListBox>
        
        <!--layer buttons-->
        <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center">
            <Button Content="Up" Click="spriteUp_Click" />
            <Button Content="Down" Click="spriteDown_Click" />
            <Button Content="Delete" Click="spriteDelete_Click" />
            <Button Content="Lock" Click="photoLock_Click" />
        </StackPanel>
        
        
        <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom">
            <Button Content="PickPhoto" Click="PickPhoto_Click" />
            <Button Content="Widget" Click="Widget_Click" />
            <Button Content="OK" Click="OK_Click" />
        </StackPanel>

    </Grid>
</Page>
